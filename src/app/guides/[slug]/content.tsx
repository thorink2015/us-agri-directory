import { ReactNode } from 'react';

/**
 * Guide content keyed by slug (matching guides.ts).
 * Add entries here when new guides are ready.
 */
export const guideContent: Record<string, ReactNode> = {};
