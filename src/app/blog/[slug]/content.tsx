import { ReactNode } from 'react';

/**
 * Blog post content keyed by the `content` field in blog-posts.ts.
 * Add entries here when new posts are ready.
 */
export const blogContent: Record<string, ReactNode> = {};
