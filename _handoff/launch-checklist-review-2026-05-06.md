# Launch checklist review — 2026-05-06

> Mark-up of `_handoff/launch-checklist.md` against current code.
> Each item is `[x] still valid`, `[~] stale, needs update`, or
> `[!] missing`. Generated by Claude Code on session
> `claude/review-codebase-docs-sJEBA`. Data only; no checklist
> rewrites — Eugen owns the source file.

## Part 1 — Build verification: pages

### Core, content pillar, regulations, authority, tools

All 28 listed pages still exist and route correctly. The folder
sweep done in T1.1 (the `/advertise` sitemap fix) confirmed every
top-level static route in `src/app/*/page.tsx`.

[x] All 28 pages valid as listed.

### Missing pages — should be added to checklist

[!] `/advertise` — shipped in PR #58 (2026-04-21)
[!] `/get-matched` — already in sitemap.ts (lead-capture page)
[!] `/affiliate-disclosure` — already in sitemap.ts
[!] `/map` — homepage map page
[!] `/guides` (index) and `/guides/<slug>` — 4 guide posts in
     `src/data/guides.ts`:
     - hire-drone-spray-operator-checklist
     - year-round-revenue-ag-drone-operators
     - how-to-become-an-agricultural-drone-pilot
     - agricultural-drone-spraying-statistics-2026
[!] `/go/<slug>` — affiliate-redirect routes (intentionally
     noindex). Don't add to verification checklist; do add to a
     "noindex routes" note so they're not counted as missing.

### Data-driven pages — counts drifted

[~] `/services/spraying (and all 9 other service slugs)` —
     **valid**, 10 services total.
[~] `/crops/corn (and all 7 other crop slugs)` — **stale**.
     Current count is **12 crops**: corn, soybeans, wheat, cotton,
     rice, grapes, orchards, cover-crops, row-crops, pasture,
     alfalfa, potatoes. Should read "and all 11 other crop slugs".
[~] `/drones/dji-agras-t50 (and all 8 other drone slugs)` —
     **stale**. Current count is **17 drones** (DJI Agras T10,
     T25, T25p, T40, T50, T100; Hylio AG-230, AG-272; XAG P100,
     P100 Pro; Talos T60X; Pyka Pelican-2; Joyance J100, J150;
     Ceres Air C31; LeadingEdge PV40x; EAVision J100). Should
     read "and all 16 other drone slugs".
[x] `/regions/corn-belt (and all 4 other region slugs)` —
     **valid**, 5 regions total.
[x] `/states/iowa (and all 49 other state slugs)` — **valid**,
     50 states.

### Blog

[x] All 10 blog post slugs match `src/data/blog-posts.ts` exactly.

### Missing dynamic pages

[!] No mention of dynamic operator pages (393 of them today).
     `src/app/operators/[slug]/page.tsx` exists; profile rendering
     should be in the verification scope.
[!] No mention of state-operator subpages
     `/states/[state]/operators` (50 routes; gated by
     `shouldIndexStateOperators`).
[!] No mention of state-crop combo pages
     `/states/[state]/crops/[crop]` (50 × 12 = 600 routes;
     gated).
[!] No mention of state-service combo pages
     `/states/[state]/services/[service]` (50 × 10 = 500 routes;
     gated).
[!] No mention of state-city pages
     `/states/[state]/[city]` (gated by `shouldIndexCity`).

### Total

The checklist's "TOTAL: ~120+ pages" line is **dramatically low**.
With 393 operator pages, 50 state operator subpages, 360 indexed
state-crop combos, 277 indexed state-service combos, 5 regions, 17
drones, 12 crops, 10 services, 50 states, 10 blog posts, 4 guides,
and ~30 static pages, the realistic indexed-page count is closer
to **1,200+**.

[~] Update total to "~1,200+ pages".

## Part 2 — Schema validation

[x] All 6 schema types still apply.
[!] **DataFeed** missing — operators index page emits a DataFeed
     schema (verify in `src/app/operators/page.tsx`).
[!] **LocalBusiness** missing — operator profile pages emit a
     LocalBusiness graph (verify in
     `src/app/operators/[slug]/page.tsx`).
[!] **Service** missing — service pages may emit Service schema.

## Part 3 — Technical SEO

| Item | Status | Notes |
|---|---|---|
| sitemap.xml generates correctly with all ~120+ URLs | [~] | Number low; sitemap is correct generation-wise after T1.1's `/advertise` fix |
| robots.txt allows all crawlers, references sitemap | [x] | `src/app/robots.ts` allows 25+ AI crawlers explicitly + default allow; references sitemap |
| IndexNow pings Bing/Yandex on deploy | [?] | Not verified; check Netlify build hooks or function |
| Google Search Console / Bing Webmaster | [?] | External setup; can't verify from code |
| Canonical URLs correct | [x] | Each page sets `metadata.alternates.canonical`; spot-checked OK |
| No duplicate titles / descriptions / H1s | [?] | Not verified this session; covered by `audit/internal-duplication.md` previously |
| All images have alt text | [?] | Not verified this session |
| No broken internal links | [?] | Last crawler run was Ahrefs 2026-04-18; superseded by April fixes |
| Core Web Vitals targets | [?] | Live measurement; performance audit at `audit/performance-audit.md` |
| Mobile responsive | [x] | Tailwind responsive utilities throughout |
| HTTPS enforced (Cloudflare) | [x] | `netlify.toml` has 301 redirect chain to https://agdronedirectory.com |
| `_memory/` and `_research/` removed from production build | [~] | Both folders are in repo but `robots.ts` has `disallow: ['/_memory/', '/_research/', '/_handoff/', '/api/', '/go/']`. Folders aren't routed by Next, so they don't ship to production anyway. Status is fine, but the checklist phrasing implies physical removal that isn't necessary. |
| `{{placeholder}}` slots resolved | [?] | Not verified; should be a Grep sweep (`Grep "\\{\\{[a-z_-]+\\}\\}"`) |

## Part 4 — Content consistency pass

| Item | Status | Notes |
|---|---|---|
| Keywords from research-05 keyword map applied | [~] | research-05-keyword-map.md is in `_research/` queue per `_memory/pending-items.md`; integration not complete |
| Internal links: 8+ per page | [?] | Spot-check; not verified this session |
| Authority links: 2+ .gov / .edu | [?] | Spot-check; not verified |
| AEO blocks (green callouts) | [x] | Pattern in `code-patterns.md` |
| FAQ schema on every content page | [?] | Most pages have it; not exhaustively verified |
| Byline + author card on every content page | [x] | `src/data/author.ts` is the canonical source |
| Last reviewed date | [~] | Some pages show; not all |
| No double dashes / em dashes | [x] | Per `_memory/standing-rules.md` and recent dash-audit work |
| No Romanian words | [x] | Last sweep was the romanian-content-audit; no recent flags |
| Operator count consistent | [x] | 393 today; single source `operators.ts` |
| Price ranges consistent | [?] | Not verified this session |
| Drone specs consistent | [?] | Not verified |

## Part 5 — Maintenance schedule

[~] Owner column lists "Claude (refresh chat)" and "Claude
     (research chat)" — these were from the pre-Claude-Code era.
     Today every cadence is owned by Claude Code via session
     prompts. Update the column to reflect current workflow.

## Part 6 — Redirect map

[~] **No `_redirects` file or `public/_redirects` exists.**
     Romanian URL redirects (`/operatori → /operators`, etc.) are
     not implemented as 301s. The `netlify.toml` redirects section
     only handles `https://www → https://` and HTTPS canonicalization.
     If any old Romanian URLs are still indexed by Google, they
     return 404 today.

     **Recommend:** verify via `gh api` or Search Console whether
     any Romanian URLs still appear in the index. If yes, add the
     12 redirects from `_handoff/launch-checklist.md` Part 6 to
     `netlify.toml`. If no impressions in 90 days, mark Part 6 as
     not applicable and remove from the checklist.

## Part 7 — Future content roadmap

[x] Roadmap items unchanged from launch-time and still relevant.

## Summary recommendations for Eugen

1. **Fix the count drift in Part 1**: 12 crops, 17 drones (not 8
   and 9). Bump the "~120+ pages" total to "~1,200+".
2. **Add the missing static routes to Part 1**: `/advertise`,
   `/get-matched`, `/affiliate-disclosure`, `/map`, `/guides` and
   the 4 guide slugs.
3. **Add a "Dynamic-page coverage" section** for operator profiles
   and the four state-combo route templates, with a note that
   each is gated by `src/lib/indexing-gates.ts` rather than
   manually verified.
4. **Resolve the Romanian-redirect question** before launch.
   Either implement the 12 redirects or document why they're
   skipped.
5. **Update Part 5's owner column** to remove the "refresh chat"
   / "research chat" terminology.
6. **Mark `_memory/` removal as not-applicable**: the folders
   never ship to production via Next.js; the `robots.txt`
   disallow is the correct enforcement layer.

None of these are launch-blocking. They're checklist hygiene that
makes a future post-launch verification pass faster.

## Verification

Counts pulled by parsing `src/data/*.ts` directly. Sitemap
content cross-referenced against `src/app/sitemap.ts`. Robots
behavior pulled from `src/app/robots.ts`. Redirect inventory pulled
from `netlify.toml`.
