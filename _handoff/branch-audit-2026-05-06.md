# Branch and PR audit — 2026-05-06

> Generated by Claude Code session
> `claude/review-codebase-docs-sJEBA`. Snapshot of every remote
> branch and open PR at the moment of audit, with deletion
> recommendations. Decision authority remains with Eugen — this file
> is data, not action.

## Summary

- **49 remote branches** total
- **36 fully merged into `main`** (safe to delete)
- **12 not merged** (live or orphaned — see breakdown below)
- **1 open PR** (#118, this session's freeze-safe fixes — draft)
- **0 other open PRs**
- **PR #106** (draft "research priority list") closed without merge
  on 2026-05-04. The same content shipped via the merged branch
  `claude/build-priority-list-e3WZt`. The orphaned source branch
  `claude/build-priority-list-K0o2R` still exists.

## Critical finding — `_memory/project-facts.md` is stale

`_memory/project-facts.md` and `CLAUDE.md` both name
`claude/add-drone-operators-directory-T0YnN` as the working branch.
That branch was **fully merged into `main`** on or before
2026-05-05 and currently has zero unmerged commits. The harness
session for this audit is on `claude/review-codebase-docs-sJEBA`
(also named in the harness instructions). One of the three open
decisions — "which branch do batch deliverables land on?" — is
therefore not a real choice between two live branches; it's a
choice between **resurrecting `T0YnN` from `main`** vs. **starting
a fresh branch**. Recommend the latter; flag this as a fix to
project-facts.md.

## Open PRs

| # | Title | Branch | State | Notes |
|---|------|--------|-------|-------|
| 118 | fix(sitemap): add /advertise + memory: research-batch override | `claude/review-codebase-docs-sJEBA` | draft | This session. Awaiting Netlify checks. |

## Unmerged remote branches (12)

Format: `last-commit-date  ahead/behind-vs-main  branch  →  recommendation`

### Live / current

- `2026-05-06  +1/-0   claude/review-codebase-docs-sJEBA` — current
  session branch, PR #118 open. **Keep.**

### Recent but stalled

- `2026-05-04  +1/-27  claude/build-priority-list-K0o2R` —
  duplicate of `claude/build-priority-list-e3WZt` which merged.
  This was the source of closed PR #106. Safe to delete after
  diffing against `e3WZt` to confirm no unique commits.
  **Recommend:** delete after diff.

### Old session leftovers (1 trivial commit ahead, hundreds behind)

These look like sessions that ran a memory note or chore commit
after the main work had merged elsewhere. Their single ahead-commit
is almost always a `memory:` or `chore:` housekeeping line. Safe to
delete; verify with `git log <branch> ^origin/main` before pruning.

- `2026-04-17  +1/-468  claude/add-drone-model-context-NVSMF` —
  "memory: mark PR #22 merged"
- `2026-04-12  +1/-472  claude/build-agri-drone-directory-K8coy` —
  "Initialize" only
- `2026-04-24  +1/-169  claude/build-guides-hub-S7TPj` —
  "chore(memory): log netlify budget…"
- `2026-04-21  +1/-221  claude/build-svg-us-map-PW07e` —
  "fix(schema): add missing BreadcrumbList…"
- `2026-04-12  +2/-472  claude/disconnect-github-repos-7e3qc` —
  Romanian-to-US conversion seed; superseded
- `2026-04-17  +1/-453  claude/review-github-repo-UC9KS` —
  "memory: log build fix session…"
- `2026-04-17  +2/-456  claude/verify-netlify-deployment-0BfOT` —
  "fix(types): remove erroneous agriculturalLandHa/vineyardHa…"

**Recommend:** delete all seven after spot-checking that each
unique commit is truly a memory note / type chore that's already
absorbed into main. Do this in one batch once the batch sweep is
green.

### Possibly meaningful unmerged work

- `2026-04-23  +5/-186  claude/build-guides-hub-jITv1` —
  "content(guides): calendar + cert stack + subscription +
  checklist". Five commits ahead. The merged
  `claude/build-guides-hub-0LIa9` (2026-04-27) likely supersedes
  but the +5 should be diffed before deletion.
- `2026-04-16  +61/-443  claude/cloudflare-api-setup-APuwy` —
  large unmerged delta (61 commits). Possibly retained
  configuration work that was reimplemented elsewhere.
  **Recommend:** Eugen confirm before deleting.
- `2026-04-16  +57/-443  claude/connect-domain-cloudflare-fTRn8` —
  "Replace domain: usagdronedirectory.com → agdronedirectory.com".
  57 commits ahead. The domain swap is on production today, so the
  work succeeded; the question is whether anything in those 57
  commits was unique. **Recommend:** Eugen confirm before deleting.

## Merged remote branches (36) — safe to prune

The 36 fully-merged branches all have zero unmerged commits and
range from `2026-04-16` (oldest, `claude/setup-domain-submission-KXRq7`)
to `2026-05-05` (newest, `claude/lead-capture-overhaul-qoohP` and
`claude/add-drone-operators-directory-T0YnN`).

GitHub's default behavior is to leave merged branches in place.
Cleanup is purely cosmetic — none of these affect builds, indexing,
or the live site. Suggested approach:

1. Wait until **after the May-16 freeze** lifts.
2. Delete merged branches via the GitHub web UI or
   `git push origin --delete <branch>` in one batch.
3. Specifically retain `main` and the active session branch.

## Local working tree

- On branch `claude/review-codebase-docs-sJEBA`
- Working tree clean (verified at audit start)
- One unpushed-then-pushed commit this session: `929bc03`
- No worktree forks or stash entries seen

## Recommended next steps for Eugen

1. **Update `_memory/project-facts.md`** to remove the "working
   branch" reference to `T0YnN` since it's merged. Replace with a
   guidance note: "The working branch rotates with each session.
   The current session's branch is named in the harness assignment.
   Don't push to `main` directly."
2. **Decide on the Tier 2 branch question** — for upcoming
   research batches, start a fresh branch (e.g.
   `claude/research-batches-2026-05`) rather than resurrecting
   `T0YnN`.
3. **Defer branch pruning** until after May 16. The cleanup
   doesn't affect ranking or revenue and the 49-branch sprawl is
   purely visual noise in GitHub's branch dropdown.
4. **Confirm or reject** the +57 / +61 cloudflare-related branches
   before deletion — they're the only meaningful unmerged delta.

## Verification

- Generated by `git for-each-ref` and `git rev-list --count`
  against `origin/main` at SHA `0b9d4d4` (PR #117 merge).
- PR list pulled via GitHub MCP `list_pull_requests` (state=open).
- Closed PR #106 referenced from `_memory/pending-items.md` and
  cross-checked against branch list.
